# Base de datos SQLite (desarrollo) — cambia a postgresql://... en producción
DATABASE_URL="file:./dev.db"

# Puerto del servidor
PORT=3000

# Entorno
NODE_ENV=development

# Orígenes permitidos (separados por coma)
ACCEPTED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4321
